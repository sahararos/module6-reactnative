# React Native Component Project

This project was created using React Native, Expo, and TypeScript.

The application demonstrates a reusable React Native component rendered in a native mobile environment.

## Features

- React Native component
- TypeScript props
- Styled UI
- Expo setup

## Run Project

```bash
npm install
npx expo start
```
# module6-reactnative
